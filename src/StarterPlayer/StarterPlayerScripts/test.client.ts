import { Result } from "@rbxts/rust-classes";
import Remotes from "ReplicatedStorage/Networking";

while(task.wait(3) !== undefined) {
	// spawn(() => {
	// 	Remotes.Test.A.Invoke(Result.ok(true));
	// });
}

